APP_PREFIX = "loans"

DATA_SIGNAL_ID = f"{APP_PREFIX}-data-signal"
FILTER_STORE_ID = f"{APP_PREFIX}-filter-store"
SELECTED_LOAN_STORE_ID = f"{APP_PREFIX}-selected-loan-store"

LAST_UPDATE_ID = f"{APP_PREFIX}-last-update"
REFRESH_BTN_ID = f"{APP_PREFIX}-refresh-btn"
RESET_FILTERS_BTN_ID = f"{APP_PREFIX}-reset-filters-btn"

REPORT_DATE_ID = f"{APP_PREFIX}-report-date"
HISTORY_DATE_RANGE_ID = f"{APP_PREFIX}-history-date-range"
COUNTERPARTY_FILTER_ID = f"{APP_PREFIX}-counterparty-filter"
CONTRACT_TYPE_FILTER_ID = f"{APP_PREFIX}-contract-type-filter"
CURRENCY_FILTER_ID = f"{APP_PREFIX}-currency-filter"
STATUS_FILTER_ID = f"{APP_PREFIX}-status-filter"

KPI_ACTIVE_LOANS_ID = f"{APP_PREFIX}-kpi-active"
KPI_TOTAL_DEBT_ID = f"{APP_PREFIX}-kpi-total-debt"
KPI_PRINCIPAL_DEBT_ID = f"{APP_PREFIX}-kpi-principal-debt"
KPI_INTEREST_DEBT_ID = f"{APP_PREFIX}-kpi-interest-debt"
KPI_WEIGHTED_RATE_ID = f"{APP_PREFIX}-kpi-weighted-rate"
KPI_DUE_30_ID = f"{APP_PREFIX}-kpi-due-30"
KPI_OVERDUE_ID = f"{APP_PREFIX}-kpi-overdue"
KPI_TOTAL_DRAWNDOWN_ID = f"{APP_PREFIX}-kpi-total-drawdown"

DEBT_DYNAMICS_CHART_ID = f"{APP_PREFIX}-debt-dynamics-chart"
COUNTERPARTY_DEBT_CHART_ID = f"{APP_PREFIX}-counterparty-debt-chart"
MATURITY_CHART_ID = f"{APP_PREFIX}-maturity-chart"
INTEREST_FLOW_CHART_ID = f"{APP_PREFIX}-interest-flow-chart"

LOANS_GRID_ID = f"{APP_PREFIX}-loans-grid"
TRANSACTIONS_GRID_ID = f"{APP_PREFIX}-transactions-grid"
SELECTED_LOAN_CHART_ID = f"{APP_PREFIX}-selected-loan-chart"
SELECTED_LOAN_TITLE_ID = f"{APP_PREFIX}-selected-loan-title"
SELECTED_LOAN_META_ID = f"{APP_PREFIX}-selected-loan-meta"

DOWNLOAD_EXCEL_BTN_ID = f"{APP_PREFIX}-download-excel-btn"
DOWNLOAD_ID = f"{APP_PREFIX}-download"

DASHBOARD_LOADING_TRIGGER_ID = f"{APP_PREFIX}-dashboard-loading-trigger"
DASHBOARD_LOADING_ID = f"{APP_PREFIX}-dashboard-loading"


DEBT_DYNAMICS_INSIGHT_ID = (f"{APP_PREFIX}-debt-dynamics-insight")
COUNTERPARTY_DEBT_INSIGHT_ID = (f"{APP_PREFIX}-counterparty-debt-insight")
MATURITY_INSIGHT_ID = (f"{APP_PREFIX}-maturity-insight")
INTEREST_FLOW_INSIGHT_ID = (f"{APP_PREFIX}-interest-flow-insight")

TRANSACTIONS_TITLE_ID = (f"{APP_PREFIX}-transactions-title")
RECONCILIATION_EXPORT_BTN_ID = (f"{APP_PREFIX}-reconciliation-export-btn")
RECONCILIATION_DOWNLOAD_ID = (f"{APP_PREFIX}-reconciliation-download")
